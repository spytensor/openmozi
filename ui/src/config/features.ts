export const UI_FEATURES = {
  // Project picker in the composer + project-scoped write permissions (P1–P3).
  projectContext: true,
  composerAttachments: true,
  composerMentions: true,
} as const;
