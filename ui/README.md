# OpenMozi Web UI

This package contains the React, Vite, and Tailwind CSS interface shared by the
OpenMozi web and desktop surfaces.

Run it from the repository root:

```bash
pnpm ui:dev
pnpm ui:build
```

See the [main README](../README.md) for setup, product documentation, and
contribution guidance.
